# triggerana_tree_protodunehd.fcl

#include "triggerana_tree.fcl"

#include "services_refactored_pdune.fcl"
#include "protodune_tools_dune.fcl"

process_name: TriggerAna_Tree_ProtoduneHD

services:   {
    @table::services_triggersim
    @table::protodunehd_reco_services
}

source:     @local::source_triggerana_tree

outputs:    @local::outputs_triggerana_tree

# This block only has analyers, accepts a file with already produced TPs, TAs, and TCs
physics:    @local::physics_triggerana_tree

